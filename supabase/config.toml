# Existing app uses custom server-side authentication; this function validates it itself.
[functions.artist-workspace]
verify_jwt = false

# Public website form. Preserve the existing public endpoint and honeypot.
[functions.booking-inquiry]
verify_jwt = false
